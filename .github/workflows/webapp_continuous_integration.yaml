# name: CI Pipeline

# on:
#   push:
#     # branches:
#     #   - 'test-ci/**'
#     #   - 'develop'
#     #   - 'main' # to remove after testing
#     paths:
#       - 'myproject/**'  # Adjust the path based on your Django project structure
#       - '.github/workflows/webapp_continuous_integration.yaml'
#   pull_request:
#       types: [opened, synchronize, reopened]
#       branches:
#           - develop
#   workflow_call:

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v2

#       - name: Cache dependencies
#         uses: actions/cache@v2
#         with:
#           path: ~/.cache/pip
#           key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
#           restore-keys: |
#             ${{ runner.os }}-pip-

#       - name: Set up Python
#         uses: actions/setup-python@v2
#         with:
#           python-version: '3.10'  # Adjust Python version as needed

#       - name: Set up virtual environment and install dependencies
#         run: |
#           python -m venv myenv
#           source myenv/bin/activate
#           pip install --upgrade pip
#           pip install -r myproject/requirements.txt

#       - name: Set environment variables
#         env:
#           DJANGO_SETTINGS_MODULE: myproject.settings
#         run: echo "Environment variables set"

#       - name: Verify manage.py file
#         run: ls myproject/manage.py  # Ensure manage.py file exists in the correct path

#       - name: Run Django checks
#         run: |
#           source myenv/bin/activate
#           python myproject/manage.py check

#       # - name: Run Django tests
#       #   run: |
#       #     source myenv/bin/activate
#       #     python myproject/manage.py test myapp --settings=myproject.settings

#       # - name: Run pytest
#       #   run: |
#       #     source myenv/bin/activate
#       #     pytest myapp/tests.py 

# #  pytest app_web --cov-fail-under=80 -vv
#-------------------------------------------------

# # # .github/workflows/integration.yml

# # name: Integration Test

# # on:
# #   push:
# #     branches:
# #       - main
# #       - develop
# #   pull_request:
# #     branches:
# #       - main

# # jobs:
# #   integration-test:
# #     runs-on: ubuntu-latest

# #     steps:
# #       - name: Checkout repository
# #         uses: actions/checkout@v2

# #       - name: Set up Python
# #         uses: actions/setup-python@v2
# #         with:
# #           python-version: '3.9'

# #       - name: Install dependencies
# #         run: |
# #           python -m pip install --upgrade pip
# #           pip install -r requirements.txt

# #       - name: Run tests
# #         run: |
# #           python manage.py test

#---------------------------------------------


name: CI Pipeline

on:
  push:
    branches:
      - develop
      - main
      - test-ci
    paths:
        - 'myproject/**'  # Adjust the path based on your Django project structure
        - '.github/workflows/webapp_continuous_integration.yaml'
  pull_request:
      types: [opened, synchronize, reopened]
      branches:
          - main
          - develop
      paths:
        - 'myproject/**'  # Adjust the path based on your Django project structure
        - '.github/workflows/webapp_continuous_integration.yaml'
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest

    # env:
    #   DATABASE_URL: "sqlite:///:memory:"

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Cache dependency # caching dependency will make our build faster.
        uses: actions/cache@v2 # for more info checkout pip section documentation at https://github.com/actions/cache
        with:
              path: ~/.cache/pip
              key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
              restore-keys: |
                  ${{ runner.os }}-pip-

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
           python-version: '3.10' 

      - name: Check Python version # checking the python version to see if 3.x is installed.
        run: python --version

      - name: Install dependencies
        run: |
          python -m venv myenv
          source myenv/bin/activate
          pip install -r myproject/requirements.txt

      - name: Run tests
        run: |
          source myenv/bin/activate
          PYTHONPATH=./ pytest myproject
